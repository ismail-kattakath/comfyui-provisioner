# RunPod Provider — TODO

Placeholder. The provisioner framework is provider-agnostic, but the RunPod-specific
bootstrap (`onstart.sh` + pod template) isn't filled in yet. Use the VastAI
provider as a reference and adapt to RunPod's lifecycle (`Container Start Command`,
persistent volume mounts, env injection via the RunPod UI/API).

When ready, contribute the implementation back via PR.
